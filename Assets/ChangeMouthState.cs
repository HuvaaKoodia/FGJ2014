using UnityEngine;
using System.Collections;

public class ChangeMouthState : MonoBehaviour {
	enum mouthState {idle, wtf, grin, smile};
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	void setTo(int i){
	
	}
}
